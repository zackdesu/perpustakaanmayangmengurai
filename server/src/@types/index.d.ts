interface User {
  username?: string;
  name?: string;
  email?: string;
  password?: string;
}

interface Payload {
  id: user.id;
  username: user.username;
  name: user.name;
  email: user.email;
}
